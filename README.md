# Разметка фото и видео в CVAT

## Что сделано

Размечены данные в CVAT для задач компьютерного зрения.
Фотографии и видео. После разметки данные выгружены
в стандартные форматы и проверены на корректность.

## Фотографии: детекция объектов

Размечены объекты на фото с помощью bounding boxes.
Классы: dog, cat, car.

После разметки данные выгружены в формате COCO JSON.

Результат:
- Изображений: 2
- Всего рамок: 3
- Собака: 1, кошка: 1, машина: 1

При открытии файла видно три блока:
images (список фото), annotations (рамки с координатами),
categories (классы). Проверено, что у каждого фото есть рамка,
а координаты не выходят за границы изображения.

Скриншоты:
- screenshots/photo_bbox_dog_cat.jpg — рамки на собаке и кошке
- screenshots/photo_bbox_car.jpg — рамка на машине

## Фото: полигональная сегментация

Размечен объект на фото с помощью полигона.
Класс: car_polygon.
Полигон обводит контур автомобиля.

Данные выгружены в формате CVAT for images 1.1 (XML).

Скриншот:
- screenshots/car_polygon.jpg — полигон на машине

## Видео: трекинг объектов

Размечено видео с собакой и кошкой. Использован режим Track,
чтобы объект отслеживался между кадрами, а не размечался заново
на каждом кадре. Для этого расставлены ключевые кадры, а промежутки
CVAT заполнил сам с помощью интерполяции.

Видео: IMG_6894.mp4, 560 кадров, 1072x1920.
Классы: dog, cat.

Результат:
- Всего треков: 2

Трек собаки:
- Кадры: 0-559
- Рамок: 489
- Ключевых кадров: 58
- Кадров, где собака ушла из кадра: 1

Трек кошки:
- Кадры: 53-469
- Рамок: 91
- Ключевых кадров: 24
- Кадров, где кошка ушла из кадра: 2

Данные выгружены в формате CVAT for video 1.1 (XML).

Скриншот:
- screenshots/video_track.jpg — разметка видео в Track Mode

## Фото: ключевые точки

Размечены ключевые точки на фото автомобиля.
Класс: car_points (тип Points).
Поставлено 8 точек на фары (по 4 на каждую).

Данные выгружены в формате CVAT for images 1.1 (XML),
так как COCO JSON не поддерживает простые Points без скелетона.

Файл: annotations/car_points.xml
Скриншот:
- screenshots/car_points.jpg — ключевые точки на фарах

## Фото: OCR-разметка

Размечен текст на фото с табличкой. Использован bounding box
с атрибутом content, в который записан распознанный текст.

Класс: ocr_text.
Распознанный текст: РОЗА ПЛЕТИСТАЯ 1800Р.

Данные выгружены в формате COCO JSON. В файле видно рамку
и атрибут content с текстом.

Скриншот:
- screenshots/ocr_text.jpg — рамка вокруг текста

## Что проверено

После выгрузки данные проверены по нескольким пунктам.

Фото (bounding boxes):
- У каждого фото есть рамка
- Координаты не выходят за границы картинки
- Классы совпадают с заданными

Полигон:
- Координаты точек полигона не выходят за границы изображения

Видео (трекинг):
- Треков ровно два — собака и кошка. Лишних нет, дублей нет.
- У каждого трека есть ключевые кадры — значит, интерполяция
  использовалась, а не ручная разметка каждого кадра.
- Моменты, когда животные уходили из кадра, отмечены.
- Пропусков нет: все кадры, где объект виден, размечены.
- Координаты рамок не выходят за границы видео.

Ключевые точки:
- Точки сохранены в файле, координаты корректны.

OCR:
- Текст сохранён в атрибуте content, рамка не выходит за границы.

## Что видно в форматах

COCO JSON (для фото и OCR) состоит из трёх частей:
- images — список изображений с размерами
- annotations — рамки с координатами [x, y, width, height]
- categories — список классов

CVAT for video 1.1 (XML) для видео:
- meta — информация о видео и классах
- track — трек объекта с уникальным id
- box — положение объекта на кадре с пометками keyframe, outside

CVAT for images 1.1 (XML) для ключевых точек и полигонов:
- image — изображение с размерами
- points — координаты точек
- polygon — координаты полигона
- label — класс объекта

## Итог

Данные размечены в CVAT и выгружены в COCO JSON и CVAT XML.
Проверено: объекты отмечены, треки не дублируются,
пропусков нет, координаты верные.

## Инструменты

- CVAT
- Форматы: COCO JSON, CVAT for video 1.1 (XML), CVAT for images 1.1 (XML)